# Как подключиться к базе данных Render (продакшн)

## 📋 Получение DATABASE_URL

### Вариант 1: Через Render Dashboard (рекомендуется)

1. Зайдите в [Render Dashboard](https://dashboard.render.com)
2. Выберите ваш **PostgreSQL** сервис (не веб-сервис!)
3. В разделе **"Connections"** найдите:
   - **Internal Database URL** (для подключения с Render)
   - **External Database URL** (для подключения с вашего компьютера)
4. Скопируйте URL (он выглядит как: `postgresql://user:password@host:port/dbname`)

### Вариант 2: Через переменные окружения Render

1. Зайдите в ваш **Web Service** на Render
2. Перейдите в **Environment** → **Environment Variables**
3. Найдите переменную `DATABASE_URL`
4. Скопируйте её значение

## 🔧 Использование скрипта

### Windows PowerShell:

```powershell
# Установите DATABASE_URL
$env:DATABASE_URL='postgresql://user:password@host:port/dbname'

# Запустите скрипт для списка всех курсов
python backend/tools/list_courses_render.py

# Или проверьте конкретный курс с видео
python backend/tools/list_courses_render.py 12
```

### Windows CMD:

```cmd
set DATABASE_URL=postgresql://user:password@host:port/dbname
python backend/tools/list_courses_render.py
```

### Linux/Mac:

```bash
export DATABASE_URL='postgresql://user:password@host:port/dbname'
python backend/tools/list_courses_render.py
```

## ⚠️ ВАЖНО: Безопасность

- **НЕ коммитьте** DATABASE_URL в Git!
- **НЕ публикуйте** DATABASE_URL в открытых местах
- Используйте переменные окружения локально
- Render автоматически использует DATABASE_URL из своих переменных окружения

## 📝 Примеры использования

### 1. Список всех курсов:

```powershell
$env:DATABASE_URL='postgresql://...'
python backend/tools/list_courses_render.py
```

### 2. Проверка видео для курса ID=12:

```powershell
$env:DATABASE_URL='postgresql://...'
python backend/tools/list_courses_render.py 12
```

## 🔍 Что покажет скрипт

- Список всех курсов с их ID и названиями
- Для каждого курса (если указан ID):
  - Список всех модулей и уроков
  - Информацию о наличии видео для каждого урока
  - Статус видео (готово для экспорта или нет)

## ❌ Если не работает

1. **Проверьте DATABASE_URL:**
   - Должен начинаться с `postgresql://`
   - Должен содержать правильные credentials

2. **Проверьте доступность базы:**
   - Убедитесь, что база данных запущена на Render
   - Проверьте, что используете **External Database URL** (для подключения с вашего компьютера)

3. **Проверьте права доступа:**
   - Убедитесь, что у вас есть права на чтение таблиц

4. **Проверьте сеть:**
   - Некоторые корпоративные сети блокируют внешние подключения
   - Может потребоваться VPN или прокси

