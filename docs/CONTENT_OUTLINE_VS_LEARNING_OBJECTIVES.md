# Разница между content_outline и learning_objectives

## 📋 content_outline (План контента)

**Что это:**
- Структура урока, создается при генерации структуры курса
- Хранится в объекте `lesson` в структуре курса
- Отображается на странице урока как "План контента"
- Это то, **что будет в уроке** (темы, разделы, практические задания)

**Пример:**
```json
"content_outline": [
  "Введение в ETL процесс: Понятие и ключевые компоненты",
  "Извлечение данных: Обзор методов извлечения",
  "Практическое задание на извлечение данных",
  "Трансформация данных: Процесс очистки",
  ...
]
```

**Где используется:**
- ✅ Отображается на странице урока
- ✅ Используется для формирования скрипта видео (ПРАВИЛЬНО)

## 🎯 learning_objectives (Цели обучения)

**Что это:**
- Цели обучения, генерируются при создании детального контента (слайдов)
- Хранится в `lesson_content` (детальный контент урока)
- Это то, **что студент должен освоить** (цели, навыки)

**Пример:**
```json
"learning_objectives": [
  "Определить ключевые компоненты ETL процесса",
  "Научиться извлекать данные из различных источников",
  "Освоить методы трансформации данных",
  ...
]
```

**Где используется:**
- ❌ НЕ должен использоваться для скрипта видео
- ✅ Используется для отображения целей обучения в детальном контенте

## 🔍 Почему они отличаются?

Это **нормально** и **правильно**, что они отличаются:

- `content_outline` = **структура урока** (что будет изучаться)
- `learning_objectives` = **цели обучения** (что студент должен освоить)

Например:
- **content_outline**: "Практическое задание на извлечение данных"
- **learning_objectives**: "Научиться извлекать данные из различных источников"

## ✅ Правильное использование для скрипта видео

**Скрипт для видео должен формироваться из `content_outline`**, потому что:
1. Это структура урока, которая отображается на странице
2. Это то, что будет озвучено в видео
3. Это соответствует ожиданиям пользователя

**НЕ должен формироваться из `learning_objectives`**, потому что:
1. Это цели обучения, а не структура урока
2. Это не то, что отображается на странице урока
3. Это не соответствует ожиданиям пользователя

## 🐛 Исправления в коде

Все места, где скрипт устанавливался из `learning_objectives`, исправлены:
- ✅ При открытии модального окна: используется `content_outline`
- ✅ При обновлении контента: используется `content_outline`
- ✅ При загрузке из кэша: используется `content_outline`
- ✅ При загрузке с сервера: используется `content_outline`
- ✅ В функции `getVideoScript()`: используется `content_outline`

## 📝 Проверка

После деплоя проверьте:
1. Откройте форму генерации видео
2. Скрипт должен содержать текст из "План контента" (content_outline)
3. Скрипт НЕ должен содержать текст из "Целей обучения" (learning_objectives)

